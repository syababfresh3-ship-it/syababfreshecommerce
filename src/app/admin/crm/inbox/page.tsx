// Inbox CRM WhatsApp — dilindungi middleware /admin (login + is_admin).
export const dynamic = "force-dynamic";

import { InboxClient } from "./inbox-client";

export default function CrmInboxPage() {
  return <InboxClient />;
}
