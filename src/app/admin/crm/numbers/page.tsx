// Urus nombor WhatsApp Official (multi-number) — dilindungi middleware /admin.
export const dynamic = "force-dynamic";

import { NumbersClient } from "./numbers-client";

export default function WaNumbersPage() {
  return <NumbersClient />;
}
