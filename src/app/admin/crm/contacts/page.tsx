// Contacts CRM — senarai + cari + tapis tag. Dilindungi middleware /admin.
export const dynamic = "force-dynamic";

import { ContactsClient } from "./contacts-client";

export default function CrmContactsPage() {
  return <ContactsClient />;
}
