// Sales Pipeline CRM — dilindungi middleware /admin.
export const dynamic = "force-dynamic";

import { PipelineClient } from "./pipeline-client";

export default function CrmPipelinePage() {
  return <PipelineClient />;
}
