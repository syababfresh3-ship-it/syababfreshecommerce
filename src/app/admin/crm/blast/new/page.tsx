// Cipta campaign Blaster (wizard 3-langkah) — dilindungi middleware /admin.
export const dynamic = "force-dynamic";

import { BlastWizard } from "../blast-wizard";

export default function NewBlastPage() {
  return <BlastWizard />;
}
